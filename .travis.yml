language: node_js
node_js:
-10.15.1
cache: yarn

install:
  - yarn install

script:
  - yarn test
  - yarn run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $NETLIFY_AUTH_TOKEN
  local_dir: build
  on:
    branch: master